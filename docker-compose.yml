services:
  bot:
    build: .
    restart: "no"
    volumes:
      # Persist the SQLite database
      - ./data:/app/data
      # Mount config (must exist before starting)
      - ./config.toml:/app/config.toml:ro
