services:
  bot:
    build: .
    restart: unless-stopped
    volumes:
      # Mount source code so /admin update can git pull
      - .:/app
      # SSH keys for git pull (adjust path if using HTTPS instead)
      - ~/.ssh:/root/.ssh:ro
